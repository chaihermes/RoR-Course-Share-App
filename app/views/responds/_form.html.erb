<%= simple_form_for([@avaliation, @avaliation.responds.build], remote: true) do |f| %>

<div class="field">
    <div class="control">
    <%# #FIXME: pesquisar como trocar o nome de um input %>
        <%= f.input :respond, input_html: { class:'textarea' }, wrapper: false, label_html: { class: 'label' } %>
    </div>
</div>

<%= f.button :submit, "Deixa a sua resposta", class:'button is-primary' %>

<% end %>