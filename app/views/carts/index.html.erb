<p id="notice"><%= notice %></p>

<h1>Carrinho</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @carts.each do |cart| %>
      <tr>
        <td><%= link_to 'Ver', cart %></td>
        <td><%= link_to 'Editar', edit_cart_path(cart) %></td>
        <td><%= link_to 'Deletar', cart, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo carrinho', new_cart_path %>
